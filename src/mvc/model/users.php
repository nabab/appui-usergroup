<?php
/*
 * Describe what it does!
 *
 **/

/** @var $this \bbn\mvc\model*/
if ( isset($model->data['limit']) ){
  
}
else if ( isset($model->data['action']) ){
  if ( $model->post['action'] === 'delete' ){

  }
  else if ( $model->post['action'] === 'update' ){

  }
  else if ( $model->post['action'] === 'insert' ){

  }
}